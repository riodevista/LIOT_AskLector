<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionBarStyle">@style/toolbar_style</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeBackground">@drawable/action_mode_background</item>
        <item name="alertDialogTheme">@style/dialog_style</item>
    </style>

    <style name="toolbar_style" parent="Widget.AppCompat.ActionBar">
        <item name="android:textColorPrimary">#fff</item>
        <item name="android:textColorSecondary">#fff</item>
        <item name="android:itemTextAppearance">@style/menuTextApearance</item>
    </style>

    <style name="dialog_style" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="dialog_button_style" parent="@android:style/TextAppearance">
        <item name="android:textColor">#000</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="menuTextApearance" parent="@android:style/TextAppearance.Widget.IconMenu.Item">
        <item name="android:textColor">@color/actionMenuTextColor</item>
    </style>

    <style name="action_mode_title_text">
        <item name="android:textColor">#fff</item>
    </style>

    <style name="action_mode_subtitle_text">
        <item name="android:textColor">#fff</item>
    </style>


</resources>
